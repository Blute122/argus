"""Real log ingestion (Phase 2).

A single pipeline normalizes, enriches, stores, detects on, and broadcasts every
event — whether it comes from the synthetic generator, syslog, the HTTP bulk
endpoint, or a tailed file.
"""
